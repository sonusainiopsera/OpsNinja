{
  "name": "@opsninja/observability",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run",
    "validate-rules": "promtool check rules alerts/realtime.rules.yaml alerts/ai-synthesis.rules.yaml || echo '[validate-rules] promtool not installed; skipping lint — run in CI with Prometheus tooling'"
  },
  "dependencies": {},
  "devDependencies": {
    "@opsninja/db": "*",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
