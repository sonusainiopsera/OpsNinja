/**
 * Queue route — agent workspace ticket queue page (WO-041).
 */

import { QueuePage } from '../../../features/queue/QueuePage';

export const metadata = {
  title: 'Queue – OpsNinja',
};

export default function Page() {
  return <QueuePage />;
}
