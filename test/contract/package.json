{
  "name": "@opsninja/contract-tests",
  "version": "0.0.1",
  "private": true,
  "description": "OpenAPI 3.1 contract test suite — validates published snapshot, TypeScript types, and real API responses (WO-099)",
  "scripts": {
    "test": "vitest run",
    "test:contract": "vitest run --reporter=verbose",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
